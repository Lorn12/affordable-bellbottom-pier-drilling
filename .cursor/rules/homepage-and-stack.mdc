---
description: Homepage-first HTML + Tailwind stack; Home and About are live.
alwaysApply: true
---

# Homepage scope and stack

This repo is HTML + Tailwind (Vite). Do not introduce WordPress, React, or another stack unless the user asks for it.

**Live now:** Home (`index.html`) and About (`about.html`), plus same-page jumps and the phone number.

**Stay “coming soon”** until the user says otherwise:

- Services (dropdown and “explore” links)
- Projects
- Careers
- Contact

Keep those items visible in the menu. Do not build remaining inner pages or invent their layouts.

When a page is approved, add it as its own HTML file and follow `.cursor/rules/code-hygiene.mdc`. Do not dump new pages into `index.html`.

**About:** implement from Figma `About Desktop` `5916:827`. There is no About tablet or mobile frame yet — do not invent those layouts. Further About visual updates stay in Figma first.

**Until the designer says otherwise:**

- Quote / “Request a Quote” stays phone (`tel:+12813523077`). No quote form on Home. A Contact page (and any form) comes later.
- Desktop **Services** is a jump to `#services`. A dropdown with a Services hub plus individual service pages comes later. Do not build that dropdown yet.
- Only change the breakpoint or section the designer named (e.g. desktop-only). Do not restyle tablet or mobile “while you are there.”

Do not change hosting or domain setup unless the user asks.

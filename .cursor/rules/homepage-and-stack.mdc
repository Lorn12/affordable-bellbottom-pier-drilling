---
description: Homepage-first HTML + Tailwind stack; Home, About, Services hub, Straight-Shaft, Contact, and Careers are live.
alwaysApply: true
---

# Homepage scope and stack

This repo is HTML + Tailwind (Vite). Do not introduce WordPress, React, or another stack unless the user asks for it.

**Live now:** Home (`index.html`), About (`about.html`), Services hub (`services.html`), Straight-Shaft Pier Drilling (`services/straight-shaft-pier-drilling.html`), Contact (`contact.html`), and Careers (`careers.html`), plus same-page jumps and the phone number.

**Stay “coming soon”** until the user says otherwise:

- Remaining individual service pages (Belled, Pool, Home Foundation, Concrete Pour — Explore links stay coming soon)
- Projects

Keep those items visible in the menu. Do not build remaining inner pages or invent their layouts.

When a page is approved, add it as its own HTML file and follow `.cursor/rules/code-hygiene.mdc`. Do not dump new pages into `index.html`.

**About:** implement from Figma `About Desktop-V1` `6546:410`, `About Large Tablet-V1` `6546:1573`, `About Regular Tablet-V1` `6552:5848`, and `About Mobile-V1` `6595:1954` on the About Page canvas. Further About visual updates stay in Figma first.

**Services:** implement from Figma `Services Desktop-V1` `6692:1699`, `Services Large Tablet-V1` `6738:2969`, `Services Regular Tablet-V1` `6738:3177`, and `Services Mobile-V1` `6738:3371` on the **Services Page** canvas. Do not restyle tablet/mobile layout on the live hub until asked to match those frames. Heading sizes on Home, About, and Services follow the Typography collection modes even when a frame is bound to the wrong style (see figma-source-of-truth). Further Services visual updates stay in Figma first.

**Straight-Shaft service page:** live file `services/straight-shaft-pier-drilling.html` from Figma on **Services Page**: Desktop-V1 `6748:4368` (1440, two-column 780 + 420), Large Tablet-V1 `6784:4838` (1280, two-column 700 + 420 from `min-[1024px]` through 1279, hamburger still below 1280), Regular Tablet-V1 `6784:5233` (768, stacked overview then full-width quote + All Services), and mobile `Services Mobile-V1` `6784:5629` (400, stacked; this is the Straight-Shaft mobile frame, not the hub). Related listing cards stay hidden. Hub `Services Mobile-V1` remains `6738:3371`.

**Contact:** live file `contact.html` from Figma on **Contact Page**: Desktop-V1 `6838:434` (1440), Large Tablet-V1 `6838:653` (1280, two-column quote from `min-[1024px]`, hamburger still below 1280), Regular Tablet-V1 `6838:863` (768, stacked), and Mobile-V1 `6838:1059` (400). Hero uses `.contact-hero-inner` (`header-xl`; 800 mobile, interpolates 742→800 from 768–1279, 800 desktop). Call-only hero CTA. Capabilities marquee after the hero. Quote section is white with photo overlay card + form; FAQ is dark-pattern; service area is white. Footer Contact lists phone, Greater Houston, and hours (Mon–Fri 7AM–6PM, Sat–Sun 9AM–6PM). No street address, email, directions link, or US-wide map until confirmed. Further Contact visual updates stay in Figma first.

**Careers:** live file `careers.html` from Figma on **Careers Page**: Desktop-V1 `6994:276` (1440), Large Tablet-V1 `6994:508` (1280, two-column apply from `min-[1024px]`, hamburger still below 1280), Regular Tablet-V1 `6994:731` (768, stacked), and Mobile-V1 `6994:940` (400). Same layout and hero heights as Contact (`.contact-hero-inner`). Hero CTA is Apply Now (`#apply`). Apply form fields: full name, email, phone, kind of work, experience. FAQ is dark-pattern. Service area copy is the crew in Hockley. Submitted state is `6994:1193`. Further Careers visual updates stay in Figma first.

**Until the designer says otherwise:**

- Homepage Equipment “Discuss Your Project,” Who We Serve “Discuss Your Project,” Project Planning “Discuss Your Project,” the bottom “Request a Quote,” and footer “Request quote” go to `contact.html`. The Call button next to that quote stays `tel:+12813523077`. Other Home quote buttons (header, hero, “Request Your Quote,” footer “Get a Quote Today”) still jump to `#quote`. About and the Services hub “Request a Quote” stay phone (`tel:+12813523077`). The Straight-Shaft page and Contact page quote forms, and the Careers apply form, open a mailto draft to `bids@affordablebellbottomdrill.com`.
- Desktop and overlay **Services** is a dropdown trigger (hover or click), not a page link. All Services in the dropdown goes to `services.html`. Straight-Shaft is live; remaining services coming soon.
- Only change the breakpoint or section the designer named (e.g. desktop-only). Do not restyle tablet or mobile “while you are there.”

Do not change hosting or domain setup unless the user asks.
